package com.flashboomlet.stocks

/**
  * Created by ttlynch on 3/30/17.
  */
class MarketInfo {

  // Return list[StockData] (not set or queue as order matters)
  def getQuote(market: String, symbol: String): Unit = {
  }

  // Return list[StockData] (not set or queue as order matters)
  def getMarketInfo(market: String, option: String): Unit = {
  }

  // Return list[StockData] (not set or queue as order matters)
  private def getMostActive(market: String): Unit = {
  }

  // Return list[StockData] (not set or queue as order matters)
  private def getMostVolatile(market: String): Unit = {
  }

  // Return list[StockData] (not set or queue as order matters)
  private def getBiggestGainers(market: String): Unit = {
  }

  // Return list[StockData] (not set or queue as order matters)
  private def getBiggestLosers(market: String): Unit = {
  }

}
